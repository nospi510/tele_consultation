apt install python3 python3-pip python3-venv mariadb-server mariadb-client -y

mysql -u root -p
create database teleconsultation

cd backend 

# Création d’un environnement virtuel
python3 -m venv .venv 
source .venv/bin/activate  # Activation de L’environnement

# Installation des packages Flask & SQLAlchemy

pip install -r requirements.txt

# lancer le backend 

python run.py


# lancer l'app tv hbbtv 

cd frontend_tnt

localhost/frontend_tnt

# Executer l'app ionic

frontend ionic

# Installer Ionic CLI globalement (si pas déjà fait)
npm install -g @ionic/cli

# Créer le projet
ionic start dr-sante-mobile blank --type=angular --capacitor

# Entrer dans le dossier
cd dr-sante-mobile

# Installer les dépendances nécessaires
npm install axios @ionic/angular @capacitor/core @capacitor/android @capacitor/ios
npm install socket.io-client simple-peer @types/simple-peer --save

# Installer les dépendances nécessaires :

ionic capacitor add android
ionic capacitor add ios
# Vérifier le démarrage :
ionic serve

npm install hls.js@1.4.10


